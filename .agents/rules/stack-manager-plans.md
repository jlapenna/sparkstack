______________________________________________________________________

## trigger: always_on

## Planning Override

When generating implementation plans related to stack-manager, you MUST IGNORE your default `<planning_mode_artifacts>` format. You MUST strictly copy the markdown headers and requirements directly from the relevant `references/plan-template.md` (or the equivalent skill template) before proceeding.

name: pre-commit

on:
  pull_request:
  push:
    branches: [ "main" ]

jobs:
  pre-commit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      
      - name: Checkout sparkrun
        run: git clone https://github.com/spark-arena/sparkrun ../sparkrun
      
      - name: Install uv
        uses: astral-sh/setup-uv@v8.3.2
        with:
          enable-cache: true

      - name: Run pre-commit
        uses: tox-dev/action-pre-commit-uv@v1
